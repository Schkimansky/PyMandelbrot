# PyMandelBrot
Mandelbrot set generator made in python.

# Usage
You can adjust the width and height to change the resolution of the image. Or change the max iteration for the mandelbrot set. Higher resolutions take longer to compute.

# Running
Run main.py to generate a mandelbrot set image. It will be saved as mandelbrot.png
